name: churchapp_flutter
description: My ChurchApp Project.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  fast_i18n: ^5.11.0
  easy_dialog: ^1.1.0
  sqflite: ^2.0.2
  dio: ^5.4.3+1
  provider: ^6.0.2
  transparent_image: ^2.0.0
  pull_to_refresh: ^2.0.0
  cached_network_image: ^3.2.0
  font_awesome_flutter: ^11.0.0
  line_awesome_flutter: ^3.0.1
  better_player_plus: 1.2.1
  youtube_player_flutter: ^9.1.3
  firebase_core: ^2.3.0
  firebase_messaging: ^14.1.3
  flutter_local_notifications: ^17.1.2
  flutter_secure_storage: ^9.2.4
  local_auth: ^2.3.0
  crypto: ^3.0.6
  shared_preferences: 2.5.5
  email_validator: "^2.0.1"
  shimmer: ^3.0.0
  #audiofileplayer: ^2.1.1
  just_audio: ^0.9.38
  just_audio_background: ^0.0.1-beta.12
  palette_generator: ^0.3.3
  file_utils: ^1.0.0
  permission_handler: ^11.3.1
  path_provider: ^2.0.9
  isolated_download_manager: ^1.0.2
  google_sign_in: ^6.2.1
  geocoding: ^3.0.0
  geolocator: ^13.0.2
  share_plus: 10.1.4
  qr_flutter: ^4.1.0
  app_links: ^6.4.1
  package_info_plus: 9.0.1
  in_app_review: ^2.0.4
  url_launcher: ^6.3.1
  lottie: ^3.1.2
  flutter_widget_from_html: ^0.16.0
  carousel_slider: ^5.0.0
  select_dialog: ^2.0.0
  #flutter_tts: ^3.2.2
  clipboard: ^0.1.3
  #translator: ^0.1.7
  flutter_sticky_header: ^0.7.0
  flutter_material_color_picker: ^1.1.0+2
  #dynamic_text_highlighting: ^2.2.0
  #media_picker_widget: ^0.0.7
  #flutter_absolute_path:
  #  git:
  #    url: https://github.com/canewsin/flutter_absolute_path.git
  smooth_page_indicator: ^1.0.0+2
  #visibility_detector: ^0.4.0+2
  file_picker: ^9.0.0
  record: ^4.4.4
  event_bus: ^2.0.0
  photo_view: ^0.15.0
  #vimeoplayer_trinity: ^0.1.9
  flutter_quill: ^11.5.1
  intl: 0.18.1
  flash: ^3.1.0
  highlight_text: ^1.3.0
  drop_cap_text: ^1.1.3
  flutter_cupertino_datetime_picker: ^3.0.0
  http_auth:
  #webview_flutter: ^4.8.0
  table_calendar: ^3.0.3
  #wakelock: ^0.2.1+1
  video_player_android: 2.9.5

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.4
  collection: ^1.15.0-nullsafety.4
  wakelock_plus: 1.5.2

  http: any
  path: any
  webview_flutter: 4.13.1
  video_player: 2.11.1
  visibility_detector: any
  mobile_scanner: ^7.2.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: any
  flutter_launcher_icons: 0.14.4

dependency_overrides:
  #flutter_inappwebview: ^5.7.2+3
  #wakelock_windows: ^0.2.1
  #win32: ^5.5.3
  #xml: ^5.0.0
  http: ^0.13.0
  #watcher: ^1.1.0
  intl: ^0.19.0
  #meta: 1.11.0
  flutter_widget_from_html_core: ^0.15.2
  value_layout_builder: ^0.5.0
  

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  remove_alpha_ios: true
  image_path: "assets/icon/triumphant_launcher.png"
  image_path_ios_dark_transparent: "assets/icon/triumphant_foreground.png"
  image_path_ios_tinted_grayscale: "assets/icon/triumphant_monochrome.png"
  adaptive_icon_background: "#00101F"
  adaptive_icon_foreground: "assets/icon/triumphant_foreground.png"
  adaptive_icon_monochrome: "assets/icon/triumphant_monochrome.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/icon/
    - assets/lottie/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: WorkSans
      fonts:
        - asset: assets/fonts/WorkSans-Regular.ttf
        - asset: assets/fonts/WorkSans-Medium.ttf
          weight: 500
        - asset: assets/fonts/WorkSans-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/WorkSans-Bold.ttf
          weight: 700
    - family: Roboto
      fonts:
        - asset: assets/fonts/Roboto-Bold.ttf
        - asset: assets/fonts/Roboto-Regular.ttf
          weight: 400
        - asset: assets/fonts/Roboto-Medium.ttf
          weight: 500

  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
